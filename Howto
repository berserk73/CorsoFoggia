Kubernetes:

kubectl apply -f demo.yml
kubectl expose deployment.apps/demo --port 8080
kubectl port-forward svc/demo 8080:8080
kubectl get all
kubectl get po
per cancellare:
kubectl delete deployments/demo
